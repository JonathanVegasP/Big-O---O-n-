// Big O Complexity:
// The function only performs a fixed number of operations regardless of the value of n.
// Since the operations are constant (three additions), the time complexity is O(1).
function sumNumber(n) {
    return n + n + n
}
